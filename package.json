{
  "name": "carespace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/normalize-food-bank-data.mjs",
    "prepare:migration": "npm run prepare:data && node scripts/generate-food-bank-migration.mjs",
    "predev": "npm run prepare:data",
    "dev": "vite",
    "prebuild": "npm run prepare:data",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
